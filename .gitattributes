# Shell scripts must keep LF endings or Git Bash cannot run them.
*.sh text eol=lf

# Mod source must keep LF endings: the compiled package's source manifest
# hashes the working-tree bytes, so a CRLF checkout fails the build gate.
*.uc text eol=lf
