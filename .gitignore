# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.venv/
venv/
env/

# Build output
build/
dist/
*.apworld
*.zip

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local-only working files, not part of the shipped world. Anchored to the repo
# root so VCD_AP/scripts (the apworld test runner) is still tracked.
/plans/
/scripts/
/spikes/
/VCD_AP/docs/

# The generated options template, copied to the repo root for inspection after
# every yaml-affecting change, and the player's own settings yaml next to it.
# The template is regenerated; the settings yaml is personal. Neither is
# committed.
/Viscera Cleanup Detail.template.yaml
/Viscera Cleanup Detail.yaml

# Mod source staged into the package by build_apworld.py. The canonical source
# is VCD_AP/mod.
/VCD_AP/apworld/data/
